apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio
spec:
  selector:
    matchLabels:
      app: portfolio
  template:
    metadata:
      labels:
        app: portfolio
    spec:
      initContainers:
      - name: lockdown-network
        image: alpine:latest
        command: ["/bin/sh", "-c"]
        args:
        - ip route add blackhole 192.168.0.0/16
        securityContext:
          capabilities:
            add: ["NET_ADMIN"]

      containers:
      - name: portfolio
        image: jafy10/portfolio 
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 1000
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp-volume
          mountPath: /tmp
        - name: next-cache
          mountPath: /app/.next/cache
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"

      volumes:
      - name: tmp-volume
        emptyDir: {}
      - name: next-cache
        emptyDir: {}
